# Copy this file to .env and fill in real values
# Never commit .env to git

ANTHROPIC_API_KEY=sk-ant-...
PAGE_ACCESS_TOKEN=EAAxx...
VERIFY_TOKEN=your_verify_token_here
DASHBOARD_PASSWORD=miasnow2024

# For local PostgreSQL on the VPS (no SSL needed for localhost):
DATABASE_URL=postgresql://miabot:CHANGE_ME_STRONG_PASSWORD@localhost/miasnow
DB_SSLMODE=disable

# For Render's hosted Postgres (keep sslmode=require):
# DATABASE_URL=postgresql://user:pass@host/db
# DB_SSLMODE=require
